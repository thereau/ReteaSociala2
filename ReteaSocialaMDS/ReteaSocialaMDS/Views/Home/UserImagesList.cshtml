@model IEnumerable<string>
@{
    ViewBag.Title = "User Images List";
}
<div class="row">
    <div class="col-md-6 col-md-offset-3">
        @foreach (var item in Model)
        {
            <div class="col-md-4">
                <img src="@Url.Content(item.ToString())" class="img-responsive" />
            </div>


        }
    </div>
</div>